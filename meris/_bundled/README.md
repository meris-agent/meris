# Bundled meris-rs (optional)

Populated at **wheel build time** via `scripts/stage_bundled_binary.py`.
Not committed — see `.gitignore`.

After `pip install`, `meris native status` should show `binarySource: bundled`.
